# Autogenerated using db-gen
# Changes to this file will be lost on next generation

defmodule KeenDocs.Database.Parsers.ResolveDocVariantParser do
  @moduledoc """
  Parser for public.resolve_doc_variant stored procedure results.
  """

  require Logger

  alias KeenDocs.Database.Models.ResolveDocVariantModel

  @spec parse_result({:ok, Postgrex.Result.t()} | {:error, any()}) ::
          {:ok, [ResolveDocVariantModel.t()]} | {:error, any()}
  def parse_result({:error, reason} = err) do
    Logger.debug("Stored procedure resolve_doc_variant returned error: #{inspect(reason)}")

    err
  end

  def parse_result({:ok, %Postgrex.Result{rows: rows}}) do
    results =
      rows
      |> Enum.map(&parse_row/1)
      |> Enum.filter(&match?({:ok, _}, &1))
      |> Enum.map(&elem(&1, 1))

    {:ok, results}
  end

  defp parse_row([code, title, show_in_path, matched]) do
    {:ok, %ResolveDocVariantModel{
      code: code,
      title: title,
      show_in_path: show_in_path,
      matched: matched,
    }}
  end

  defp parse_row(_row) do
    Logger.warning("Invalid row structure in resolve_doc_variant result")
    {:error, :invalid_row}
  end
end